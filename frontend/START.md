# 🚀 Brzi start - Frontend

## Pokretanje (jednostavno!)

```bash
cd frontend
npm start
```

To je to! Aplikacija će se otvoriti na `http://localhost:3000`

## Prvi put?

Ako prvi put pokrećeš, prvo instaliraj dependencies:

```bash
cd frontend
npm install
npm start
```

## Napomene

- Backend mora biti pokrenut na `http://localhost:8000`
- Vite automatski proxy-uje `/api` pozive na backend
- Hot reload radi automatski - promene se vide odmah

## Problemi?

Ako ima problema:
1. Obriši `node_modules` i `package-lock.json`
2. Pokreni `npm install` ponovo
3. Pokreni `npm start`
